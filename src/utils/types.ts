export type TUser = {
  accountId: string;
  caseUid: string;
  creationDate: string;
  publicId: string;
  status: string;
  reference: string;
};

export type TObject = {
  [key: string]: string;
};
